# Server-side IndexCore base URL.
#
# Read only by the Node.js server process (never bundled into the browser).
# The Reference Web talks to IndexCore exclusively through this origin using
# the read-only /v1 HTTP Query Contract.
INDEXCORE_BASE_URL=http://127.0.0.1:8080